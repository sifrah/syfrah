services:
  ipfs:
    image: ipfs/kubo:latest
    environment:
      - IPFS_PROFILE=server
    ports:
      - "5001:5001"
    networks:
      mesh:
        ipv4_address: 172.28.0.2

networks:
  mesh:
    ipam:
      config:
        - subnet: 172.28.0.0/16
